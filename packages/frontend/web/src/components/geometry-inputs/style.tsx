import styled from "@emotion/styled";
import { Button } from "@mui/material";

export const GeometryInputShowButtonsContainer = styled("div")`
  display: flex;
`;

export const GeometryInputShowButton = styled(Button)``;
